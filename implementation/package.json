{
  "name": "vaxguard-blockchain",
  "version": "1.0.0",
  "description": "VaxGuard smart contracts — Polygon Amoy testnet",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy":  "npx hardhat run scripts/deploy.js --network amoy",
    "test":    "npx hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.19.0"
  }
}
